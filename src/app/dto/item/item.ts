export class Item {
    public id: number;
    public title: string;
    public details: string;

    constructor(id: number, title: string, details: string) {
      this.id = id;
      this.title = title;
      this.details = details;
    }
}

